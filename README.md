# Personal-Workspace-App
**Personal Workspace** is a local-first, dark-themed documentation/notes editor — no AI, no account, no cloud. It saves your notes as a `workspace.json` file directly on your own machine, with optional LAN or internet (Cloudflare tunnel) access when you need it.
